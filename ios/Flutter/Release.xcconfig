#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
#include "Generated.xcconfig"

// bundle ID (APP_BUNDLE_IDENTIFIER) は共有 Extension と共有するため AppBundleIdentifier-Release.xcconfig で定義する。
// Release.xcconfig を継承する Profile config も同じ prod 値を使う。
#include "AppBundleIdentifier-Release.xcconfig"
